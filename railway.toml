[build]
buildCommand = "python manage.py collectstatic --noinput"

[deploy]
startCommand = "python manage.py migrate --noinput && gunicorn point.wsgi --workers 3 --timeout 30 --preload --bind 0.0.0.0:$PORT"
