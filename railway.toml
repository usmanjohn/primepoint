[build]
builder = "NIXPACKS"
buildCommand = "python manage.py collectstatic --noinput"

[deploy]
# One entry point for every service built from this repo; it branches on ROLE
# (cron → telegram_daily, otherwise the web server). See start.sh for why.
startCommand = "bash start.sh"
